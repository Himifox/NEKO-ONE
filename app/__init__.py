"""Server packages retained by the public-room edition.

Importing :mod:`app` is intentionally side-effect free. The previous desktop
runtime installed Agent, plugin, browser and launcher bindings here; the public
server and memory server now initialize only their own dependencies.
"""
